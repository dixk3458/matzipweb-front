import { MoonLoader } from 'react-spinners';
import { colors } from '../../../constants';

function CustomLoadingSpinner() {
  return <MoonLoader color={colors.BLUE} />;
}

export default CustomLoadingSpinner;
